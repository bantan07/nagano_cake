<p id="notice"></p>

<div class="container">  
 <div class="row">  
  <div class="px-3 mx-auto">
   <h4>マイページ</h4>
   <h4>登録情報</h4>
   <%= link_"編集する", edit_customer_path %>
   <table border="1">
    <tr>
     <th>氏名</th>
      <td>
       <%= @customer.last_name %>
       <%= @customer.first_name %>
     </td>
    </tr>
    <tr>
     <th>フリガナ</th>
      <td>
      <%= @customer.last_name_kama %>
      <%= @customer.first_name_kana %>
      </td>
    </tr>
    <tr>
     <th>郵便番号</th>
     <td><%= @customer.postal_code %></td>
   </tr>
     <th>住所</th>
     <td><%= @customer.address %></td>
    <tr>
     <th>電話番号</th>
      <%= @customer.telephone_number %>
    </tr>  
    <tr>
     <th>メールアドレス</th>
     <td><%= @customer.email %></td>
    </tr> 
   </table>
    <h4>配送先</h4>
    <%= link_to "一覧を見る", addresses_path, class:'btn btn-primary' %>
    <h4>注文履歴</h4>
    <%= link_to "一覧を見る",  orders_path, class:'btn btn-primary' %>
    <% end %>
  </div>
 </div>
</div>
