<p id="notice"></p>
<h4>新規会員登録</h4>
<div class=container>
 <div class=row>
  <div class="px-3 mx-auto">
    <%= form_with model:@customer, url: new_customer_registration_path, local:true,method: :patch do |f| %>
    
    <div class="form-group">
     <label for="name">名名
     (性)<%=f.text_field :last_name %>
     (名) <%=f.text_field :first_name %>
     </label>
   </div>
   <div class="form-group">
    <label for="name_kana">フリガナ
     (セイ)<%=f.text_field :last_name_kana %>
     (メイ)<%=f.text_field :first_name_kana %>
    </label>
    <div class=form-group>
     <label for="email">メールアドレス
      <%= f.text_field :email %>
     </label>
   </div>
   </div>
   <div class="form-group">
     <label for="postal_code">郵便番号(ハイフンなし)
      <%= f.text_field :postal_code %>
    </label>
   </div>
   <div class=form-group>
     <label for="address">住所
      <%= f.text_field :address %>
     </label>
   </div>
   <div class=form-group>
     <label for="telephone_number">電話番号(ハイフンなし)
      <%=  f.text_field :telephone_number %>
     </label>
    </div>
  <div class=form-group>
     <label for="password">パスワード(6文字以上)
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> characters minimum)</em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "new-password" %>
    </label>
  </div>

  <div class=form-group>
     <label for="password_confirmation">パスワード(確認用)
    <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
     </label>
 </div>
   
    <%=f.submit "新規登録" %>
   
    <h4>既に登録済みの方</h4>
    <%= link_to "こちら", new_customer_session %>
  　<h4>からログインしてください。</h4>
  　<% end %>
  </div>
 </div>
</div>
